#include <main.h>
/**
 *main - check code for putout
 *
 *void funtion
 *
 *Return: Always 0.
 */
int _isalpha(int c);
{
	if (ch >= 97 && ch <= 122)
	{
		return (1);
	}
	else if (ch >= 65 && ch <= 90)
	{
		return (0);
	}
	return (0);
}
