#include <main.h>
/**
 *main - check code for putout
 *
 *void funtion
 *
 *Return: Always 0.
 */
int _abs(int);
{	int n, abs_n;
	if (n > 0)
	}
		return (n);
	}
	else if (n < 0)
	{
		abs_n = n * -1;
		return (abs_n);
	}
	return (0);
}
